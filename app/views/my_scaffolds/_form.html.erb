<%= form_for(@my_scaffold) do |f| %>
  <% if @my_scaffold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_scaffold.errors.count, "error") %> prohibited this my_scaffold from being saved:</h2>

      <ul>
      <% @my_scaffold.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :contact %><br>
    <%= f.text_area :contact %>
  </div>
  <div class="field">
    <%= f.label :result %><br>
    <%= f.text_area :result %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
